// SCI GreenTrack — Netlify + Google Sheets Edition
// No secret is stored in the browser. The Netlify Function reads secrets from Netlify Environment Variables.
window.SCI_CONFIG = {
  API_ENDPOINT: '/.netlify/functions/api'
};
